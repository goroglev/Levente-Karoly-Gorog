package com.avg.app_similarity.rwr.graph;

import java.util.Map;

public class CategoryNode extends Node {
	
	public CategoryNode(final String id, final Map<Node, Double> features) {
		super(id, features);	
	}
	
}
